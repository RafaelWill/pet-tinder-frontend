import { PopularityFilterPipe } from './popularity-filter.pipe';

describe('PopularityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
